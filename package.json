{
    "author": "Hector del Rio",
    "license": "MIT",
    "dependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-latest": "^6.24.1",
        "body-parser": "^1.18.2",
        "express": "^4.16.2",
        "knex": "^0.14.0",
        "mysql2": "^1.5.0",
        "nodemon": "^1.12.1",
        "socket.io": "^2.0.4"
    },
    "scripts": {
        "migrate": "docker exec strategos yarn run knex migrate:latest",
        "migrate:down": "docker exec strategos yarn run knex migrate:rollback",
        "migrate:history": "docker exec strategos yarn run knex migrate:currentVersion",
        "make-certs": "docker exec strategos sh scripts/make-certs.sh",
        "babel-node": "babel-node --presets=latest",
        "start": "nodemon --exec yarn run babel-node -- ./src/index.js",
        "build": "babel src -d dist"
    },
    "babel": {
        "presets": [
            "latest"
        ]
    }
}
